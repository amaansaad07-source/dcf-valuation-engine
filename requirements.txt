# Core valuation engine
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0
matplotlib>=3.7.0
requests>=2.31.0
numpy-financial>=1.0.0
openpyxl>=3.1.0

# Web app
streamlit>=1.50.0

# Market data (optional — the engine falls back to manual overrides if unavailable)
yfinance>=0.2.40
